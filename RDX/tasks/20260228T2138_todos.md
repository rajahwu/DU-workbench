# DUDAEL Workbench — TODOs
**Created**: 2026-02-28T21:38:01Z
**Updated**: 2026-03-02T23:46:00Z

---

## Infrastructure

- [x] Run `pnpm install` in `workbench/` to install declared dependencies
- [x] Verify Vite config exists in `web/react_app/` with path aliases resolved
- [x] Verify tsconfig.json exists in `web/react_app/` with `paths` mapping
- [x] Uncomment `phases` in `pnpm-workspace.yaml` — now an active workspace with `workspace:*` dep
- [x] Add `@data` path alias in vite.config.ts + tsconfig.json for data layer imports
- [ ] Add `.env.example` with required Supabase env vars (SUPABASE_URL, SUPABASE_ANON_KEY)
- [x] Standardize CSS naming: some shells use `styles.css`, others `style.css` (DoorShell, LevelShell)

## Type Alignment

- [x] Reconcile the two `PhasePacket` types — unified in `phases/types.ts`, `manager.ts` now imports from it
- [x] `pool.ts` now imports from `../types`
- [x] Define `Card` type for draft system — `DraftCard` schema in `data/cards/types.ts`, pool in `data/cards/pool.ts`
- [x] Define `VesselData` type for vessel registry — `VesselConfig` + `VesselMechanics` in `data/entities/vessels/vessels.ts`
- [ ] Eliminate remaining `any` types: ~~`shuffle()` in DraftOffering~~ (fixed in v2), `location.state` cast in DraftReckoning

## Phase Implementation

- [x] Wire `title-screen.ts` export: `mountTitleScreen(host, onEnterDudael)` with cleanup
- [x] Implement phase 02 select: 5-vessel selection with sigils, stats, theology, lock
- [x] Implement phase 03 staging: architecture diagram, live engine stats, loop visualization
- [x] Implement phase 04 draft: Light/Dark keeper card draft with reroll (3-stage: Approach → Offering → Reckoning)
- [x] Implement phase 05 level: 3×3 tap mini-game, timer, health, depth scaling
- [x] Implement phase 06 door: 3 paths (Light/Dark/Secret) with alignment requirements
- [x] Implement phase 07 drop: run summary, mock unlocks/scars, loop back to staging
- [ ] Implement `hourglass_router/` — the 9-House Descent/Ascent controller
- [X] Decide title experience: PixiJS animation (`TitlePage.tsx`) vs HTML/CSS (`TitleShell`) — currently `TitleShell` is routed, `TitlePage.tsx` is orphaned

## Data Layer

- [x] Extract vessel engine data from `SelectShell` → `data/entities/vessels/vessels.ts` (already existed)
- [x] Wire `buildVesselPacketStats()` into SelectShell Lock Vessel flow
- [x] Extract card pool from `DraftShell` → `data/cards/pool.ts` (typed `LIGHT_POOL` / `DARK_POOL`)
- [x] Wire `getDraftPoolCounts()` into DraftOffering for vessel-aware pool composition
- [x] Wire `getLevelCombatDeltas()` into LevelShell for vessel-aware combat math
- [x] Wire `getDoorCosts()` + `getSecretDoorThreshold()` into DoorShell
- [x] Wire `shouldIncrementMetaCounter()` into DropShell for meta progression
- [ ] Extract level configuration (names, types, difficulty scaling) to data layer — still inline arrays in LevelShell
- [ ] Create `data/lore/` content (theology text, keeper lines currently hardcoded in shells)
- [ ] Expand card pool beyond 10 prototype cards — design more cards with full visibility manifests + vessel interactions
- [X] Promote `data/` to workspace package when CLI/API consumers need it

## Game Systems

- [ ] Implement `game/core/` — engine entry point
- [ ] Implement `game/systems/` — entity system, vessel system, candy system (per `candy.md` design)
- [ ] Implement `game/cartridges/01_dudael_9pad/` — first real cartridge (current LevelShell is a prototype tap game)
- [ ] Implement `game/interfaces/` — standardized input/output contracts for cartridges
- [ ] Wire drafted cards into actual gameplay effects in Level (currently cards are carried in packet.meta but not applied)

## State Management

- [ ] Set up Redux store with game slice (alignment, inventory, candy state)
- [ ] Replace manual `localStorage.getItem("dudael:active_packet")` calls with `readActivePacket()` from `@du/phases`
- [ ] Wire TanStack Query for Supabase data fetching
- [ ] Wire Supabase auth for user identity (full path exchange)

## Web Platform

- [x] Set up React Router 7 route definitions for all 7 phases
- [x] Integrate `PhaseRouterBridge` into RootLayout
- [ ] Build shared component library per `tree.md`: VesselCard, BiasBar, PhaseHeader, etc.
- [ ] Set up Storybook stories for shared components
- [x] Integrate Tailwind CSS (installed but not imported in any shell — all shells use custom CSS) — Done (v2 UI FORGE). Wired via `@tailwindcss/vite` + `index.css`. TitleScreen migrated to Tailwind.
- [ ] Migrate remaining 6 phase CSS files to Tailwind (iterative — see v2 suggestions)
- [ ] Split phase shells into Shell/Screen pattern — Done (v2 UI FORGE). All 7 phases split.
- [ ] Fix DraftShell store access violations — Done (v2 UI FORGE). Sub-routes now use Outlet context.

## Testing

- [ ] Add Vitest unit tests for `manager.ts` (legal transitions, illegal transitions, side effects)
- [ ] Add Vitest unit tests for `meta.ts` (snapshot/restore, inventory stacking, alignment parity)
- [ ] Add Vitest unit tests for `boot.ts` (packet validation, recovery flow)
- [ ] Add Vitest unit tests for `title-exchange.ts` (full path, lite path)
- [ ] Add Vitest unit tests for data layer helpers (`buildVesselPacketStats`, `getDraftPoolCounts`, `getLevelCombatDeltas`, `getDoorCosts`, `shouldIncrementMetaCounter`)
- [ ] Add Playwright browser tests for phase loop (title → select → staging → draft → level → door → drop → staging)
- [ ] Set up Storybook visual tests via `@storybook/addon-vitest`

## Other Consumers

- [ ] Implement `cli/terminal_app/` — terminal consumer using `routerCLI()` adapter
- [ ] Implement `api/routes/` — API consumer
- [ ] Set up `db/schema/` and `db/migrations/` (Supabase migrations)
- [ ] Set up `config/environments/` (dev, staging, prod configs)

## Code Quality

- [x] Move `dev-walk.ts` from `phases/` to `web/react_app/` — now imports from `@du/phases`
- [ ] Add ESLint config if not present in `web/react_app/`
- [ ] Set up typedoc generation for `@du/phases`
- [ ] Make meta.ts testable (add `resetRunMeta()` or factory pattern)
